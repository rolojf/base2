baseurl = "https://rolo.tips/"
title = "Mi página matona"
languageCode = "es"
paginate = "5" # Number of posts per page
googleAnalytics = "" # Enable Google Analytics by entering your tracking id
disableKinds = ["section", "taxonomy", "taxonomyTerm", "RSS", "sitemap", "robotsTXT", "404"]

[Author]
  name = "Rolando Flores"
  bio = "Aquí se supone pongo mi bio"
  avatar = "img/avatar.png"

[Params]
  subtitle = "En Cabeza Ajena" # Subtitle of your site
  description = "Salvo que quieras encontrar atajo" # Descrip del sitio a como sale en google
  opengraph = false
  g_analytics = false
  twitter_cards = false
  readmore = false # Show "Read more" button in list if true
  authorbox = true
  post_navigation = true
  post_meta = ["date", "categories"] # Order of post meta information
  mainSections = ["post"]

[Params.sidebar]
  widgets = ["recent", "categories", "taglist"]

[Params.widgets]
  recent_num = 5 # Set the number of articles in the "Recent articles" widget
  tags_counter = false # Enable counter for each tag in "Tags" widget (disabled by default)

[markup]
  [markup.goldmark]
    [markup.goldmark.renderer]
      unsafe = true
